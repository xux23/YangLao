# 基于 Spring Boot 的养老机构管理系统 —— 方案汇总

> 日期：2026-08-16　｜　配套文档：《开题报告-基于SpringBoot的养老机构管理系统.docx》

## 一、技术栈

### 后端
| 技术 | 用途 |
|------|------|
| Spring Boot 3.x | 主框架（JDK 17+） |
| MyBatis-Plus | 数据持久层，简化 CRUD |
| MySQL 8.0 | 业务数据存储 |
| JWT（jjwt 或 hutool） | 登录认证，无状态令牌 |
| EasyExcel | 数据导出 Excel（名单导出、记录导出） |
| Maven | 项目构建 |

### 前端
| 技术 | 用途 |
|------|------|
| Vue 3 + Vite | 框架 + 构建工具（Node 16+） |
| Element Plus | UI 组件库 |
| Vue Router + Pinia | 路由与登录态管理 |
| Axios | 前后端请求 |
| ECharts | 统计图表 |

### 工具与环境
IntelliJ IDEA（后端）、Visual Studio Code（前端）、Navicat（数据库）、Postman（接口测试）、Git（版本管理）。
部署：一台电脑装 JDK + MySQL 即可，无 Redis、无中间件。

## 二、功能模块（5 个）

| 模块 | 功能点 | 使用角色 |
|------|--------|----------|
| 1. 系统管理 | 用户管理、角色权限配置、个人中心（改密码）、操作日志、首页数据看板与统计图表 | 管理员 |
| 2. 老人档案管理 | 老人基本信息维护、入住/退住登记、房间床位分配、老人名单导出 | 管理员、护理人员 |
| 3. 护理管理 | 护理计划制定与执行、日常护理记录、护理记录导出 | 护理人员 |
| 4. 健康管理 | 健康档案、体征数据记录、用药计划与每日用药任务（提醒闭环） | 护理人员维护；家属只读 |
| 5. 探访管理 | 探访预约与审批、家属留言反馈 | 家属提交；管理员/护理人员审批 |

### 角色与权限（JWT + 接口拦截）
- **管理员 admin**：全部功能（含用户管理、操作日志、统计看板）
- **护理人员 nurse**：老人档案、护理、健康维护、探访审批；无用户管理
- **家属 family**：仅查看自己关联老人的健康档案、提交探访预约与留言；不可修改任何业务数据

## 三、数据库表（8 张）

| 表名 | 关键字段 | 说明 |
|------|----------|------|
| sys_user | id, username, password（BCrypt）, real_name, role（admin/nurse/family）, phone, status, create_time | 三类角色共用，角色用字段 |
| elder_info | id, name, gender, birthday, id_card, phone, emergency_contact, emergency_phone, room_no, bed_no, health_summary, status（在住/已退住）, checkin_time, checkout_time, family_id（关联家属账号）, create_time | 房间/床位用字段，不另建表；一个老人关联一个家属账号 |
| care_record | id, elder_id, plan_name（护理项目）, plan_frequency（频次）, care_content, nurse_id, care_time, remark | 护理计划与记录合一 |
| health_record | id, elder_id, blood_pressure, heart_rate, temperature, blood_sugar, record_time, recorder_id, remark | 每次体征一条记录 |
| medicine_plan | id, elder_id, medicine_name, dosage, plan_date, plan_time, status（待执行/已执行/已逾期）, remark | 一行 = 某老人某天某个时间点的一次用药任务；每日任务按需生成，不单独建表 |
| visit_appointment | id, elder_id, family_id, visit_date, visit_time, persons, status（待审核/已通过/已驳回/已完成）, remark, create_time | 探访预约与审批 |
| message | id, elder_id, family_id, content, reply, reply_time, status（未回复/已回复）, create_time | 家属留言与机构回复 |
| sys_log | id, user_id, username, operation（操作描述）, method（接口路径）, params（请求参数）, ip, create_time | 操作日志，AOP 切面自动记录，仅管理员可查 |

### 表关系
- elder_info 1—N care_record / health_record / medicine_plan / visit_appointment / message（老人表是业务主表）
- sys_user 1—1 elder_info（家属账号通过 elder_info.family_id 关联）
- sys_user 1—N care_record（nurse_id 记录操作人）；sys_log N—1 sys_user（记录操作者）

## 四、后端接口（约 45 个，详见《需求/API接口文档.md》）

1. **认证**：登录（签发 JWT）、获取当前用户信息、修改密码
2. **用户管理**：用户分页查询、新增、修改、删除、重置密码
3. **操作日志**：日志分页查询（按操作人/时间筛选，仅管理员）
4. **老人档案**：分页列表、详情、新增、修改、删除、入住登记、退住登记、名单导出（Excel）、健康档案查询与修改
5. **护理**：护理记录分页列表、新增、修改、删除、记录导出（Excel）
6. **健康**：体征记录增删改查、用药计划新增/列表/停用、当日任务查询（按需生成）、任务确认执行、逾期列表
7. **探访**：预约提交、预约列表、审核通过/驳回、标记完成
8. **留言**：留言发表、留言列表、机构回复
9. **统计**：看板总览、老人年龄分布、近 30 天护理/探访趋势、某老人体征趋势（供 ECharts 用）

## 五、开发顺序建议

1. 数据库建表 + 初始化数据（1 天）
2. 后端：登录认证与统一响应/异常处理 → 用户管理 → 操作日志（AOP 切面，越早加越好）→ 老人档案（含导出）→ 护理（含导出）→ 健康 → 探访 → 留言 → 统计（3~4 周）
3. 前端：登录页与路由守卫 → 各模块页面 → 首页看板图表（3~4 周）
4. 联调、功能测试、补数据、演示（1~2 周）

## 六、答辩可讲的点

- JWT 无状态认证 + 三角色接口权限控制（拦截器 + 注解）
- 基于 AOP 的操作日志：一个切面自动记录所有写操作，不侵入业务代码，操作可追溯
- 用药任务"计划—执行—反馈"闭环（按需生成当日任务、逾期自动标记提醒）
- ECharts 健康趋势与运营看板
- EasyExcel 数据导出，替代传统 POI 手写样式
- 统一响应体 + 全局异常处理，接口规范
- 表设计取舍：小数据用字段不拆表（角色、房间、用药任务），减少联表复杂度